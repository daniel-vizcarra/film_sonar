<!DOCTYPE html>
<html>
  <head>
    <title>FilmSonar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# stylesheet_link_tag "application" fue reemplazado por la línea de sass de abajo si usas propshaft %>
    <%# Si usas propshaft (default en Rails 7+ sin webpacker): %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%# Si por alguna razón tuvieras una configuración más antigua o con webpacker/jsbundling, la línea podría ser diferente %>
    <%# pero para una instalación nueva con --css sass, la de arriba debería funcionar con importmaps. %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <h1>FilmSonar</h1>
      <nav>
        <ul>
          <li><%= link_to "Inicio", root_path %></li>
          <%# Aquí irán más enlaces de navegación %>
        </ul>
      </nav>
    </header>

    <main>
      <%# Aquí se mostrarán los mensajes flash (notificaciones) %>
      <% flash.each do |type, msg| %>
        <div class="flash_<%= type %>">
          <%= msg %>
        </div>
      <% end %>

      <%= yield %> <%# El contenido de cada página específica se inserta aquí %>
    </main>

    <footer>
      <p>&copy; <%= Time.current.year %> FilmSonar. Todos los derechos reservados.</p>
    </footer>
  </body>
</html>